name: my_own_chess
description: Chess....

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  copy_with_extension: ^4.0.0
  provider: ^6.0.2
  equatable: ^2.0.3
  flutter_svg: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: 2.1.7
  copy_with_extension_gen: ^4.0.0

flutter:
  uses-material-design: true
